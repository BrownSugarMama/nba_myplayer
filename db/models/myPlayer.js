const mongoose = require('./models/myPlayer.js')

mongoose.model()
module.exports = myPlayer

const myplayer = {
    "point guard",
    "shooting guard",
    "small forward",
    "power forward",
    "center",
}
